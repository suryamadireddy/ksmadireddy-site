# Docs

Markdown specs and handoff documents. **Not loaded by Next.js** — the live site uses `app/<route>/page.tsx` (and co-located UI under that folder when needed, e.g. `app/atelier/`).

| File | Purpose |
|------|---------|
| [`atlas-spec.md`](atlas-spec.md) | Atlas product spec (future app forked from Meridian) |
| [`atelier-page-build-spec.md`](atelier-page-build-spec.md) | Atelier `/atelier` page build handoff |
